import React from "react"
import Layout from "../components/public-components/Layout"

const NotFoundPage = () => (
  <Layout>
    <h1>Page non trouvée</h1>
    <p>La page ciblée ne semble pas exister.</p>
  </Layout>
)

export default NotFoundPage
